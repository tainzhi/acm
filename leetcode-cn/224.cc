class Solution {
public:
    int calculate(string s) {
        stack<char> stack;
        for (auto c: s) {
            if (c == '(') {
                stack.push(c);
            }
            
        }
    }
};